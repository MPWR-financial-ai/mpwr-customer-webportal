# Environment (local, dev, prod)
VITE_ENVIRONMENT=local

# API Configuration
VITE_API_BASE_URL=https://api.mpwr.com

# AWS Cognito Configuration (Customer Pool)
# Variable names match CircleCI context
# For local development, defaults to dev pool if not set
VITE_COGNITO_CUSTOMER_POOL_ID=us-east-1_xxxxxxxxx
VITE_COGNITO_CUSTOMER_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_COGNITO_CUSTOMER_DOMAIN=your-domain.auth.us-east-1.amazoncognito.com

# Copy this file to .env.local for local development
# CircleCI will create .env during build with values from context
